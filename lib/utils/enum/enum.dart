enum Status { loading, error, completed }

enum RequestStatus { pending, accepted, rejected, upcoming, complete }

enum PaymentStatus { pending, completed, failed }

enum ServiceStatus { pending, accepted, rejected, upcoming, complete }

enum StatusType { pending, accepted, rejected, upcoming, complete }
